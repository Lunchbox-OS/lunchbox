[package]
name = "lunchbox-management-macros"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Proc-macros for lunchbox-management: turn the ManagementService trait into a JSON-RPC dispatcher"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full"] }
quote = "1"
proc-macro2 = "1"
