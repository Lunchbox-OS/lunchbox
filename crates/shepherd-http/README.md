# shepherd-http

HTTP management API server for Shepherd.

## Overview

Exposes a local/LAN JSON-RPC endpoint that lets a parent or administrator manage
Shepherd from a phone or browser on the same network, without needing direct
access to the launcher UI.

The surface is deliberately two endpoints:

| Method | Path | Purpose |
|--------|------|---------|
| `POST` | `/api/v1/rpc` | JSON-RPC dispatch into every `ManagementService` method. |
| `GET`  | `/api/v1/events` | Server-Sent Events stream of every `shepherd_api::Event`. |

An optional Bearer token can be configured for authentication (see below).

## RPC wire format

Requests:

```json
{
  "method": "<wire-name>",
  "params": <object | null>
}
```

Successful responses are 2xx with the trait method's return value as the body —
whatever `dispatch_json` produces. Failures are 4xx/5xx with

```json
{ "error": "<code>", "message": "<string>" }
```

The `<code>` is machine-readable (`method_not_found`, `invalid_params`,
`not_found`, `bad_request`, `forbidden`, `conflict`, `unprocessable`,
`internal`), the message is human-friendly.

### Method names

The canonical list is `docs/rpc-schema.json` (auto-generated from the
`ManagementService` trait — the same file the Kotlin/TypeScript client
codegen consumes). At time of writing:

- Health & state: `health`, `service_state`, `ping`
- Entries: `list_entries`, `get_entry`
- Sessions: `current_session`, `launch`, `stop_current`, `extend_current`
- Groups: `list_groups`
- Overrides: `list_overrides`, `get_override`, `upsert_override`, `delete_override`
- Tokens: `adjust_tokens`
- Usage: `usage_all`, `usage_entry`
- Volume: `get_volume`, `set_volume`, `set_mute`, `volume_up`, `volume_down`, `toggle_mute`
- Brightness: `get_brightness`, `set_brightness`, `brightness_up`, `brightness_down`
- Config: `reload_config`
- User: `logout`
- Debug: `list_windows`, `act_on_window`

Adding a new method is a one-place trait change on `ManagementService`; the
`#[management_rpc]` proc-macro re-emits `dispatch_json`, the schema is
regenerated by `cargo run -p shepherd-management --bin rpc-codegen`, and every
transport (HTTP, IPC, BLE) picks it up for free.

### Example — `curl` a launch

```sh
curl -X POST http://localhost:8080/api/v1/rpc \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <token>' \
  -d '{"method": "launch", "params": {"id": "steam-monument-valley"}}'
```

Response on approval:

```json
{ "Approved": { "session_id": "...", "deadline": "2026-07-02T18:30:00-04:00" } }
```

or, on policy denial:

```json
{ "Denied": { "reasons": [ { "code": "quota_exhausted", ... } ] } }
```

These are `LaunchOutcome`'s serde-externally-tagged variants.

### Result wrapping

A few methods on the trait carry `#[rpc(wrap_result = "field")]` so their
returns show up as an object with a named field instead of a bare primitive
(forward-compat with adding more fields). Concretely:

- `extend_current` → `{ "new_deadline": <timestamp | null> }`
- `delete_override` → `{ "deleted": <bool> }`
- `reload_config` → `{ "entry_count": <number> }`

## Auth

`AuthSources` combines two token sources:

- A static token from `[service.management_api].auth_token` in the config file.
- A dynamic token from a plugged-in `AdminAuthority` (in production this is the
  BLE claim machine: the token is minted when a companion phone pairs and
  becomes the admin).

The middleware runs on both `/rpc` and `/events`. If neither source is
configured — no static token *and* no BLE admin has claimed yet — the surface
runs in open mode. As soon as either exists, `Authorization: Bearer <token>`
is required.

## SSE

`GET /api/v1/events` streams the full `shepherd_api::Event` firehose:
`SessionStarted`, `SessionEnded`, `StateChanged`, `VolumeChanged`,
`BrightnessChanged`, `PolicyReloaded`, and so on. The stream stays open until
the client disconnects.

The `EventSource` browser API can't set custom headers, so the SSE handler
also accepts the token as a `?token=` query parameter.

## Integration

The HTTP server is started by shepherdd when `[service.management_api].enabled
= true`. Config knobs:

```toml
[service.management_api]
enabled = true
port = 8080
bind = "0.0.0.0"          # or 127.0.0.1 for local-only
auth_token = "..."         # optional Bearer token
bind_retry_seconds = 300   # keep retrying if the addr isn't up yet
```

## Testing

Integration tests in `tests/api.rs` cover auth, RPC method dispatch,
per-method result shapes, and error mapping. The e2e crate
(`crates/shepherd-e2e`) drives the same endpoint end-to-end against a real
shepherdd process.
