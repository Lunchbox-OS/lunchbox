[package]
name = "lunchbox-widgets"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "GTK4 widgets shared by the lunchbox launcher and HUD"

[dependencies]
lunchbox-util = { workspace = true }

gtk4 = { workspace = true }
chrono = { workspace = true }
