[package]
name = "lunchbox-media"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Linux media-library launcher binary built on lunchbox-media-core"

[[bin]]
name = "lunchbox-media"
path = "src/main.rs"

[dependencies]
lunchbox-media-core = { path = "../lunchbox-media-core" }
lunchbox-media-ui = { path = "../lunchbox-media-ui" }
# `clap` feature exposes the shared `Quality` enum as a `--quality` value type.
lunchbox-media-app = { path = "../lunchbox-media-app", features = ["clap"] }
# The on-disk video cache, shared with lunchboxd (issue #127).
lunchbox-media-cache = { path = "../lunchbox-media-cache" }
lunchbox-util = { workspace = true }

clap = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
nix = { workspace = true }

eframe = { version = "0.34", default-features = false, features = ["default_fonts", "glow", "wayland", "x11"] }
egui = "0.34"
egui_extras = { version = "0.34", features = ["image"] }
glow = "0.17"
image = { version = "0.25", default-features = false, features = ["jpeg", "png", "webp"] }
gilrs = "0.11"
raw-window-handle = "0.6"
ureq = { version = "2.10", default-features = false, features = ["tls"] }
url = "2.5"

[dev-dependencies]
# Only for the test that pins this CLI's flag spellings to the mirrors in
# `lunchbox_api::EntryKind::Media`, which lunchboxd builds argv from (#127).
lunchbox-api = { workspace = true }
tempfile = { workspace = true }
