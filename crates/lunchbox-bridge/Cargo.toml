[package]
name = "lunchbox-bridge"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared output backend for the input-compat bridges: compositor-agnostic synthetic input via /dev/uinput"

[dependencies]
anyhow = { workspace = true }
tracing = { workspace = true }
evdev = "0.13"
