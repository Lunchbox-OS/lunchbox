#!/usr/bin/env bash
# Development wrapper - thin convenience script for developers
# Equivalent to: lunchbox dev run

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/lunchbox" dev run "$@"
